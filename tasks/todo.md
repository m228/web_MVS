# TODO — код-ревью (2026-07-04)

Новый раунд ревью всего бэкенда через 3 агента `code-reviewer` (ядро / API+сеть / инфраструктура).
Отсортировано по приоритету. Коммиты — по одному на пункт, автор m228.
Прошлый раунд (2026-07-02, 21 пункт) — ниже в архиве.

---

## 🔴 Критично

- [ ] **#N1 Гонка `force_close` vs `generate` (GigE)** — `camera_core.py:932` vs `1044`
  `generate()` пишет `self.ia = ia` и `self.running = True` без блокировки; `force_close()`
  читает/обнуляет `self.ia` тоже без лока. uvicorn обслуживает `/stream` и `/force_close`
  в разных потоках → поток B может уничтожить acquirer между `self.ia = ia` и `ia.start()`,
  и `start()` уйдёт на мёртвый acquirer. Фикс: короткий lock на секцию
  `[self.ia=…; self.running=True; ia.start()]` либо проверка `self.running` перед стартом.

- [ ] **#N2 Утечка локального `capture` в `RtspCameraWorker.generate`** — `camera_core.py:1363`
  При параллельном `force_close`, обнулившем `self.capture`, ветка `self.capture is not None`
  ложна → `release()` локального `capture` не вызывается → утечка сокета + потока декодирования
  FFmpeg. Фикс: независимый `if capture is not None: capture.release()` в `finally`, как для `ia`.

- [ ] **#N3 Мутация `worker.interface_id` в обход `select_interface`** — `app.py:147,162,363`
  `/api/ip`, `/api/get_network_settings`, `/api/camera/info` пишут `worker.interface_id = …`
  напрямую. Фронт долбит их параллельно → один меняет интерфейс, второй читает изменённое
  состояние и открывает камеру не через тот интерфейс. Фикс: передавать interface/handle
  параметром в `get_ip()/get_network_settings()/get_info()`, не мутируя общее состояние.

- [ ] **#N4 Потеря backup при зависании apply-скрипта** — `updater.py:277`
  `Remove-Item $backup` и `$staging` — безусловные. Если PowerShell-хелпер убьют/зависнет
  после удаления backup, но до старта нового exe — приложение мертво, восстановить нечем.
  Фикс: удалять backup только после успешного `Start-Process`; хранить его в
  `DATA_DIR/.update/backup`, а не в `%TEMP%`.

---

## 🟠 Средние

- [ ] **#N5 Update-эндпоинты без аутентификации** — `app.py:96`
  `/api/update/download` и `/apply` (последний делает `os._exit(0)`) публичны на `0.0.0.0:8000`.
  Любой в сети может инициировать перезапуск. Фикс: ограничить до localhost по `request.client.host`.

- [ ] **#N6 Опасный паттерн подстановки в PowerShell** — `net_tools.py:64`
  `_FILTER_MATCH` вставляется в скрипт через `.replace("%MATCH%", …)`. Сейчас это константа
  (дыры нет), но паттерн опасен. Фикс: вынести в PS-переменную внутри скрипта; добавить
  валидацию имени адаптера (`^[\w\s\-\.]+$`) на стороне Python.

- [ ] **#N7 Скачивание апдейта: нет лимита размера и таймаута на трансфер** — `updater.py:181`
  `timeout=180` — socket-таймаут, не на весь трансфер (медленный поток висит вечно).
  Плюс нет лимита размера файла (может забить диск на промышленном ПК).
  Фикс: сверять `Content-Length` c `asset["size"]`, читать чанками со счётчиком/таймаутом.

- [ ] **#N8 Нет валидации диапазонов в `/api/camera/stream`** — `app.py:278`
  `fps=0.001`, `width=-1` уходят прямо в `generate()`. Фикс: `Query(gt=0)` для fps/width/height;
  заодно `int = None` → `int | None`.

- [ ] **#N9 `rtsp_save` не валидирует URL по схеме** — `app.py:212`
  Проверка `rtsp/rtsps` есть только при стриминге, не при сохранении → мусор попадает в базу.
  Фикс: та же проверка схемы в `rtsp_save`.

- [ ] **#N10 `rtsp_store.save/remove` теряет базу при ошибке чтения** — `rtsp_store.py:47`
  `load()` глотает исключение и возвращает `[]`, после чего `save()` перезаписывает всю базу
  одной камерой. Фикс: `load()` пробрасывает ошибку, `save()/remove()` прерываются без записи.

- [ ] **#N11 `snapshot()` читает живой `last_frame` без copy** — `camera_core.py:1378`
  `cv2.imencode` может получить numpy-массив, параллельно перезаписываемый `generate()`.
  Фикс: `img = self.last_frame; ... img.copy()` перед кодированием.

- [ ] **#N12 Проглоченное исключение без деталей** — `camera_core.py:1216`
  `except Exception:` логирует строку без типа/текста → диагностика GenTL-кодов невозможна.
  Фикс: `as e`, добавить `repr(e)` + `_explain_error(e)` в payload.

---

## 🟡 Мелочи

- [ ] **#N13 `int(fps)` молча режет дробный fps** — `camera_core.py:798` (комментарий или `float`).
- [ ] **#N14 `on_video` возвращает строку `"1"/"2"`** вместо числа/булева — camera_core.py.
- [ ] **#N15 `set_advanced()` — GET с именем сеттера** — `app.py:189` (переименовать/задокументировать).
- [ ] **#N16 `powershell` без полного пути** — `updater.py:301` (PATH hijacking; абсолютный путь).
- [ ] **#N17 Двойной вызов `is_admin()`** — net_tools.py (лишний процесс PS на действие).
- [ ] **#N18 `ping_device` не валидирует IP** — `camera_core.py:1875` (`socket.inet_aton` до вызова).
- [ ] **#N19 Магические числа в цикле ожидания apply** — `updater.py:244` (вынести в PS-переменные).

---

## Review — что сделано (2026-07-04)

_(заполняется по мере выполнения)_

---
---

# TODO — код-ревью (2026-07-02) [АРХИВ]

Итоги ревью через агентов (ядро / апдейтер / FastAPI / фронтенд). Отсортировано по приоритету.
Критичные пункты проверены вручную по коду, помеченные `[проверить]` — со слов агента, требуют подтверждения.

---

## 🔴 Критично — ломает функциональность

- [x] **#1 Смена только IP не применяется** — `camera_core.py:1179-1186` ✅ fb43b9c
  При смене одного IP (без маски/шлюза) `advanced_settings` становится `False` (стр. 1138),
  код пишет IP в nodemap (стр. 1177), но падает в `else` и делает `return` на стр. 1186 —
  **до `DeviceReset.execute()` (стр. 1189)**. Без ресета камера persistent-IP не подхватывает.
  Фикс: убрать ранний `return` в `else`; ресет должен выполняться, если `ip_changed`.

- [x] **#2 Инвертированная логика «IP занят»** — `camera_core.py:1160-1164` ✅ 9b021ff
  `device_busy = ping_device(ip)` → `if not device_busy: return "ip_busy"`.
  Отсутствие ответа на пинг трактуется как «занят» — логика вывернута, реальный конфликт
  IP пропускается. Плюс пинг крутится под `_control_lock`, блокируя control-операции на таймаут.
  Фикс: выправить логику (ответил на пинг = занят) или убрать проверку; вынести пинг из-под лока.

---

## 🟠 Важно

- [x] **#3 Дедлок в `_change_ip_locked` (подтверждён, был безусловный)** — `camera_core.py:1129` ✅ 807e5a3
  Зовёт `get_network_settings()` внутри секции, держащей `_control_lock`; `threading.Lock`
  не реентрантный. Проверить: реально ли `get_network_settings` повторно берёт тот же лок
  и спасает ли кэш. Если да — заменить `_control_lock` на `RLock`.

- [x] **#4 Апдейтер — path traversal при распаковке** — `updater.py:113`
  `zf.extractall(STAGED)` без фильтрации имён → архив с `..\..\` пишет файлы за пределы папки.
  Апдейтер работает от админа. Фикс: проверять `dest.resolve()` внутри `STAGED` перед распаковкой
  (или `filter='data'` на Python 3.12+). Также санировать `asset["name"]` (стр. 104) через `Path(name).name`.

- [x] **#5 Апдейтер — нет проверки целостности zip** — `updater.py:110`
  Скачанный архив не сверяется с хэшем/подписью, распаковывается вслепую. Фикс: класть SHA256
  в релиз, считать хэш скачанного zip и сравнивать до `extractall`.

- [x] **#6 Апдейтер — нет отката при частичном обновлении** — `updater.py` / `update.ps1`
  Между удалением старого exe и копированием нового есть окно → при сбое можно остаться без
  рабочего exe. Фикс: бэкап `_internal\`+exe во temp, восстановление при ошибке `Copy-Item`.
  Смежное: `Stop-Process -Force` + фикс. `Start-Sleep 500ms` → заменить на `Wait-Process`.

- [x] **#7 Апдейтер — инъекция через пути в генерируемом PowerShell** — `updater.py:145-150`
  Пути инлайнятся f-строкой без экранирования; апостроф в пути ломает скрипт.
  Фикс: экранировать `'` → `''` или передавать как параметры скрипта.

- [x] **#8 rtsp_store — неатомарная запись + гонка** — `rtsp_store.py:25-29` ✅ d5f289d
  `write_text` прямо в целевой файл → сбой на середине = битый JSON и потеря всех RTSP-камер.
  Read-modify-write без лока. Фикс: `os.replace` через temp-файл + модульный `threading.Lock`.

- [x] **#9 Фронтенд — `setInterval` без ID, не остановить** — `page/static/js/multi.js:683` ✅ 95c9c90
  `startMetricsPolling` не сохраняет ID интервала. Вред меньше (страница выгружается целиком),
  но паттерн выбивается из camera.js/rtsp.js. Фикс: сохранять ID, `clearInterval` в `beforeunload`.

- [x] **#10 Фронтенд — XSS в конфиге камеры** — `page/static/js/camera.js:713` ✅ 4103df5
  `${String(cfg[key])}` в `innerHTML` без `escapeHtml` (в multi.js/index.js экранирование есть).
  Данные из GenICam-нодмапа. Фикс: обернуть в `escapeHtml(...)`.

- [x] **#11 Фронтенд — нет таймаутов на fetch** — `page/static/js/api.js:14` ✅ 7885866
  Ни один запрос не имеет `AbortController`/таймаута → при зависшем бэкенде UI виснет
  (особенно во время рестарта на обновлении). Фикс: `AbortController` + `setTimeout(abort)`.

---

## 🟡 Мелочи / техдолг

- [x] **#12 Хардкод путей `"page/..."`** — `app.py:38` и обработчики ✅ 37c6939
  Строки вместо `BUNDLE_DIR / "page/..."` — противоречит CLAUDE.md, ломается при другой CWD.
- [x] **#13 RTSP url не валидируется по схеме** — `app.py` (SSRF: `file://`/`http://` в `cv2.VideoCapture`). ✅ 884413b
- [x] **#14 `select_interface`-эндпоинт не логирует на входе** — выбивается из соглашения проекта. ✅ ef1a9f9
- [x] **#15 Дублирование `formatDuration`/`showSavePath`** — camera.js/multi.js/rtsp.js. ✅ dec1084
- [x] **#16 Временный `web_mvs_apply_*.ps1` не подчищается** из `%TEMP%`. ✅ 37b4de7
- [x] **#17 GigE `generate()` — возможные утечки acquirer** `[проверить]` — `camera_core.py:~905, ~1031` ✅ fea779e
  Агент указал на double-destroy при гонке с `force_close()` и утечку при провале `apply_settings`.
  Требует внимательной проверки логики `self.ia is ia` в `finally`.

---

## 🟡 Доп. находки (повторное ревью ветки `update`, 2026-07-02)

- [x] **#18 `download_latest` дважды дёргает GitHub API** — `updater.py:95` ✅ 35e9217
  `check_latest` уже получил метадату, `download_latest` зовёт `_fetch_latest_meta()` снова →
  между вызовами релиз мог смениться, скачается не та версия, что показана в UI.
  Фикс: сверять tag / переиспользовать метадату.
- [x] **#19 Строковое сравнение версий** — `updater.py:78` ✅ 6e97277
  `_norm(current) != latest` считает апдейт доступным при любом несовпадении, включая даунгрейд.
  Фикс: сравнивать как кортежи чисел (semver).
- [x] **#20 Мёртвая ссылка `updateLatest`** — `index.js:1018` ✅ 89cd7c2
  `getUpdateEls().latest` = `getElementById('updateLatest')`, но элемента нет в `index.html`.
  Фикс: убрать обращения к `latest`.
- [x] **#21 `pollUntilBack` пуллит `/api/update/check`** — `index.js:1137` ✅ 1dd51a2
  Health-check после apply бьёт в эндпоинт, синхронно ходящий на GitHub (timeout 15 c).
  Фикс: пинговать лёгкий `/api/debug/info`.

---

## Review — что сделано

Все 21 пункт исправлены, по одному коммиту на пункт (автор m228). Проверка:
- Python: `py_compile` camera_core / app / updater / rtsp_store — OK
- JS: `node --check` api / camera / multi / index / ui / rtsp — OK
- semver-логика (#19) прогнана юнит-проверкой — OK
- Камеру/драйвер локально проверить нельзя (см. CLAUDE.md) — нужен прогон на Server:
  смена IP (#1-3), стрим/утечки (#17), обновление из бандла (#4-7,16,18,19).

Не входило в ревью и осталось как есть: mock-данные DA123123, костыли genicam.
