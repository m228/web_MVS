Это сборник в котором будут кейсы для решения багов, сюда записывается баг и потом он решается и потом commit

1. bug: Камера (GigE Vision, Hikrobot MV-CS050-10GC) не подключается — "раньше работало".

   Симптом: при подключении create() падает с
     GenTL exception: Requested operation not implemented. (Message from the
     source: <мусор>) (ID: -1003)  -> приходило как UnicodeDecodeError.

   ИСТИННАЯ ПРИЧИНА (вскрыл traceback из diag.py):
     Падает не update() и не сам .cti, а внутри harvesters:
       ImageAcquirer.__init__ -> module.register_event(event_type)
       -> genicam.gentl System/Device.register_event -> UnicodeDecodeError.
     1) Hikrobot-продюсер НЕ реализует register_event для модулей System/Interface/
        Device -> GenTL -1003. harvesters это штатно ловит как NotImplementedException
        и пропускает (см. core.py ImageAcquirer.__init__: try/except NotImplemented).
     2) НО genicam 1.5.1, формируя текст ошибки из не-UTF-8 байтов продюсера, бросает
        UnicodeDecodeError ВМЕСТО NotImplementedException. harvesters его не ловит ->
        __init__ обрывается -> create() падает.
     Баг ПЛАВАЮЩИЙ: мусорные байты иногда оказываются валидным UTF-8, тогда проходит
     (поэтому в diag STAGE 3 проходил, а STAGE 1/2 — нет).

   Почему "раньше работало":
     Установлена DEV-сборка harvesters 0.post.dev64 (в requirements.txt — 1.4.3!).
     Между dev63 и dev64 регистрацию событий расширили на System/Device, и оно
     начало падать на Hikrobot. venv собран не из релиза.

   ФИКС (код, camera_core.py): _patch_genicam_register_event()
     Оборачиваем genicam.gentl System/Interface/Device.register_event так, чтобы
     UnicodeDecodeError превращался обратно в NotImplementedException — и harvesters
     сам его проглатывал. DataStream не трогаем (там события кадров, fetch работает).
     Патч ставится при импорте camera_core, пишет в лог camera_core.compat.

   ГИГИЕНА окружения (желательно): привести venv в соответствие requirements.txt
     pip install --force-reinstall --no-cache-dir "harvesters==1.4.3"
     (проверить: pip show harvesters -> 1.4.3, а не 0.post.devNN).

   Доп. диагностика: _explain_error (читаемый текст), log_environment (версии/пути),
     _discover_cti (выбор продюсера), diag.py (3 стадии).

   Как проверить:
     - python diag.py: STAGE 1/2/3 -> все OK (в логе будет camera_core.compat
       "Включена совместимость событий GenTL").
     - В приложении "Подключить" на DA5896461 -> поток идёт.



2. bug: Камера (GigE) подключается и стартует, но "нет кадров" — независимо от
   выбранного размера пакета поток запускается, потом сразу таймауты.

   Симптом: ia.start() проходит, но ia.fetch() ловит таймаут (-1011) по кругу
   ("слишком много таймаутов получения кадра"), картинки нет.

   ВЕРОЯТНЫЕ ПРИЧИНЫ (по убыванию):
     1) TriggerMode=On. Камера осталась в режиме триггера (часто после MVS) —
        acquisition стартует, но без триггер-сигнала камера НЕ отдаёт кадры ->
        fetch() = таймаут. Размер пакета тут ни при чём (поэтому "не важно сколько
        пакет выбираешь"). ФИКС (camera_core.apply_settings): перед стартом
        принудительно ставим AcquisitionMode=Continuous и TriggerMode=Off (обёрнуто
        в try, т.к. имена нод/режимы зависят от прошивки).
     2) Большой GVSP-пакет без jumbo-кадров на адаптере -> UDP-пакеты теряются ->
        кадр не собирается -> -1011. Лечится включением jumbo на странице
        "Сеть камер" (net_tools) ИЛИ меньшим packet_size.
     3) Две GigE-камеры одновременно: один control-канал/полоса не тянут два
        потока — второй стартует, кадров нет. В мультипросмотре теперь
        ограничение: одна активная GigE, при старте второй — подтверждение и
        закрытие первой (multi.js askReplaceGige/confirmReplaceGige).
     4) Брандмауэр блокирует входящий GVSP (UDP) — кадры не доходят.
     5) Камера в чужой подсети/за маршрутизатором — потеря пакетов (см. ForceIP).

   !!! ГЛАВНАЯ ПРИЧИНА РЕГРЕССИИ (нашли по сравнению с 02f71aa "6.7", когда работало):
       multicam-рефактор начал принудительно ставить ia.num_buffers = 24. До него
       num_buffers НЕ трогался (дефолт harvesters ~3) и одиночная камера была
       стабильна. Для 5 МП RGB8 один буфер ≈ 15 МБ -> 24 буфера ≈ 360 МБ; продюсер
       Hikrobot на таком пуле отдаёт ~5 кадров и сваливается в сплошные -1011
       ("хватает на 5 кадров и потом падает", не зависит от размера пакета).
       ФИКС: убрали переопределение num_buffers (вернули дефолт). Несколько GigE
       одновременно всё равно запрещены в UI, поэтому большой пул не нужен.
       Также: при packet=1500 кадры шли, при 8164 нет, хотя на NIC jumbo=9014 ->
       jumbo не работает сквозняком (фильтр-драйвер не привязан / свитч без jumbo /
       нужен реконнект адаптера). До рефактора packet_size вообще НЕ задавался —
       камера держала рабочее значение. Поэтому для проверки: оставить поля
       "Формат" и "Размер пакета" ПУСТЫМИ -> приложение их не трогает = поведение
       как до регрессии.

   ФИКСЫ В КОДЕ (camera_core.py / app.py / фронтенд):
     - УБРАН принудительный num_buffers=24 (корень "5 кадров и падает").
     - Цикл таймаутов кадра возвращён к простой до-multicam логике 02f71aa "6.7"
       (убраны WARMUP_MAX_TIMEOUTS / first_frame — это была multicam-правка d4e07ce).
     - Замки _control_lock/_registry_lock ОСТАВЛЕНЫ: они защитные (не дают параллельным
       запросам фронта ловить -1005 AccessDenied), на стрим одиночной камеры не влияют.
     - AcquisitionMode=Continuous + TriggerMode=Off перед стартом (apply_settings).
     - GevSCPD (межпакетная задержка) — добавлен параметр packet_delay в stream ->
       generate -> apply_settings; есть поле в UI (camera.html, чипы multi). ГЛАВНЫЙ
       рычаг против burst-потерь: размазывает отправку пакетов кадра во времени.
     - _to_bgr теперь дебайерит Bayer (_BAYER_TO_BGR) и переводит RGB8 -> BGR.
       Bayer втрое легче RGB8 по трафику -> меньше потерь.
     - num_buffers подбирается под размер кадра (_choose_num_buffers,
       STREAM_BUFFER_BUDGET), а не фикс. 24 (для 5МП RGB8 это было ~360 МБ).
     - Пошаговая настройка: см. НАСТРОЙКА_КАМЕРЫ.md.

   Как проверить (на Server):
     - Подключить одну камеру -> поток идёт стабильно; в логе apply_settings
       нет ошибок по AcquisitionMode/TriggerMode.
     - Если всё ещё -1011: формат BayerRG8, включить jumbo, поднимать GevSCPD
       (1000 -> 2000 -> 5000) пока "ОШИБКИ" не перестанут расти.





---------------------------------------------------------------------------
N. bug: В собранном exe камеры НЕ видны (список пуст / 0 GigE-устройств),
   хотя из PyCharm (запуск из исходников) те же камеры видны и работают.
   Актуально с ранних версий (1.0.2/1.0.3 и далее); admin/не-admin — без разницы.

   Симптом: exe грузит продюсер («Драйвер загружен»), cti_source=env:GENICAM_GENTL64_PATH
   (системный MVS), runtime найден — но device_count=0. В UI только mock-камера.

   ДИАГНОСТИКА (добавлен режим exe --diag + логи потока/числа камер):
     - Тот же frozen exe в --diag (главный поток, БЕЗ uvicorn) перечисляет 10 камер
       и открывает их. Значит заморозка/бандл/DLL/продюсер — исправны (проба ctypes
       грузит GenApi_*_MV, GCBase, MvCameraControl, MvRender — все OK).
     - Как сервер: и стартовый скан (lifespan, MainThread), и /api/cams (AnyIO worker)
       давали 0. Пробник A/B/C до uvicorn показал: свежий Harvester.update() в главном
       потоке «прогревает» продюсер на весь процесс.

   ИСТИННАЯ ПРИЧИНА:
     GenTL-продюсер Hikrobot во frozen-сборке требует, чтобы его первая
     инициализация (первый Harvester.update()) прошла в ГЛАВНОМ потоке. Иначе он
     инициализируется так, что из рабочих потоков uvicorn перечисляет 0 устройств.
     Из PyCharm первый update() исторически шёл в подходящем контексте, поэтому там
     баг не всплывал. Это НЕ версии (harvesters 1.4.3, genicam 1.5.1 одинаковы),
     НЕ env, НЕ DLL, НЕ права.

   ФИКС (run.py):
     _warmup() перед uvicorn.run(): создаёт одноразовый Harvester, add_file(.cti),
     update() в главном потоке, reset(). Эффект глобальный на процесс — дальше
     сканирование из любых потоков (в т.ч. AnyIO worker) видит все камеры.
     Диагностический инструмент оставлен: web_MVS.exe --diag (см. diag.py, run.py).

   Как проверить (на Server): собрать build.bat -> web_MVS.exe (обычный режим) ->
     в UI камеры на месте (кол-во > 1, реальные серийники), в логе run.warmup
     device_count>0. Подтверждено на 1.1.0.
